{
  "name": "api-express-mongo",
  "version": "1.0.0",
  "description": "Simple Api expres Mongodb",
  "main": "app.js",
  "scripts": {
    "dev": "concurrently -k -r -s first \"npm run dev:server\" \"npm run lint:watch\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:server": "NODE_ENV=development nodemon app.js",
    "lint": "esw . --config=.eslintrc.json",
    "lint:watch": "npm run lint -- --watch"
  },
  "author": "Fadly Aidiel",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^3.0.6",
    "body-parser": "^1.19.0",
    "concurrently": "^5.0.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^9.0.0",
    "mongodb": "^3.3.3",
    "mongoose": "^5.7.7",
    "multer": "^1.4.2",
    "validator": "^11.1.0"
  },
  "devDependencies": {
    "eslint": "^6.6.0",
    "eslint-watch": "^6.0.1"
  }
}
